<!doctype html>
<html lang="en">

<%- include("component/head") %>

<body>
  <% if(obj.fileName){ %>
  <form id="myform" method="POST" action="/login?n=<%= obj.fileName %>">
  <% }else{ %>
  <form id="myform" method="POST" action="/login">
  <% } %>
    <table class="gbt2">
      <tbody>
        <tr>
          <td>Login</td>
        </tr>
        <tr>
          <td>account:</td>
          <td><input type="email" name="account" id="inputEmail" placeholder="Email address"></td>
        </tr>
        <tr>
          <td>password:</td>
          <td><input type="password" name="password" id="inputPassword" placeholder="Password"></td>
        </tr>
        <tr>
          <td><button type="submit">Sign in</button></td>
        </tr>
      </tbody>
    </table>
  </form>
  <script>
    document.getElementById("myform").onsubmit = (evt) => {
      var ele = document.getElementById("inputPassword");
      /// TODO:Jerry: no encription at the moment;
      //ele.value = md5(ele.value);
    };
  </script>
  <script src="/lib/custom/md5.min.js" defer></script>
</body>

</html>