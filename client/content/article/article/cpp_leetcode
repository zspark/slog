由易到难刷leetcode

索引
1. LC15 single-number
1. LC45 maximum-depth-of-binary-tree
1. LC49 same-tree


1. LC15 single-number

```cpp
/*题目描述
现在有一个整数类型的数组，数组中素只有一个元素只出现一次，其余的元素都出现两次。
注意：
你需要给出一个线性时间复杂度的算法，你能在不使用额外内存空间的情况下解决这个问题么？*/

/*
 * @param A int 整型一维数组
 * @param n length of int A
 * @return int
 * */

/*
 * 相同两个数异或为0 不同异或为1
 * 0异或一个数为那个数本身
 * 异或运算可交换
 * 最后结果即出现一次的数
 * */

int singleNumber(int* A,int n)
{
	int result = A[0];
	for(int i = 1;i < n;++i)
		result = result ^ A[i];
	return result;
}
```

2. LC45 maximum-depth-of-binary-tree


```cpp
/*题目描述
求给定二叉树的最大深度，
最大深度是指树的根结点到最远叶子结点的最长路径上结点的数量。*/

/**
 * struct TreeNode {
 *	int val;
 *	struct TreeNode *left;
 *	struct TreeNode *right;
 * };
 */

class Solution {
public:
    /**
     * 
     * @param root TreeNode类 
     * @return int整型
     */
    int maxDepth(TreeNode* root) {
        // write code herej
		if(root == NULL) return 0;
		int left = maxDepth(root->left);
		int right = maxDepth(root->right);
		return max(left,right)+1;
    }
};
```

3. same-tree

```cpp

```